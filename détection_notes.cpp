#include "detect_notes.h"
#include <string>

std::string detect_notes(const std::string& filename) {
    // Ici tu mettras ton code FFT / analyse audio
    return "Notes détectées dans " + filename;
}
