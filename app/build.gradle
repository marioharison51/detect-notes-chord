plugins {
    id 'com.android.application'
}

android {
    namespace 'com.detectnotes.chord'
    compileSdk 34

    defaultConfig {
        applicationId "com.detectnotes.chord"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.7.0'
}
